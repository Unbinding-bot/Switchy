name: switchy
description: A single-screen ESP controller
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.0
  wifi_iot: ^0.3.25
  path_provider: ^2.0.11
  file_picker: ^5.2.2
  fl_chart: ^0.60.0
  csv: ^5.0.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
